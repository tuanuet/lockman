# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]

- Align repository docs and release metadata with the published `v1.0.0` root module release.
- Prepare nested module `v1.0.0` tags for `backend/redis`, `idempotency/redis`, and `guard/postgres`.

## [1.0.0] - 2026-03-30

- Release the stable `github.com/tuanuet/lockman` root SDK module.
- Publish the user-first `Run` and `Claim` SDK path with example-driven docs and adapter modules.
