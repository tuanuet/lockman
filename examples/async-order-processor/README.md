# Async Order Processor Example

This runnable example moved to the Redis idempotency adapter module.

Run it from the adapter module root:

```bash
cd idempotency/redis
go run ./examples/async-order-processor
```
